name: Secrets
# Create a workflow that triggers on dispatch
# 1. Print our slogan
# 2. Guess our magic number

on: 
  workflow_dispatch

# 1
jobs:
  print-slogan:
    runs-on: ubuntu-latest
    env:
      SLOGAN: ${{ vars.SLOGAN }}
    steps:
    - run: echo ${{ vars.SLOGAN }}
    - run: echo $SLOGAN
  
# 2
  guess-magic-number:
    runs-on: ubuntu-latest
    env:
      MY_MAGIC_NUMBER: ${{ vars.MY_MAGIC_NUMBER }}
    steps:
    - name: Guess Magic Number
      run: echo ${{ secets.MY_MAGIC_NUMBER}}
    - name: Guess Magic Number With Environment Variable
      run: echo $MY_MAGIC_NUMBER
      
